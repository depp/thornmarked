load("@rules_cc//cc:defs.bzl", "cc_binary")
load("//base:copts.bzl", "COPTS")

cc_binary(
    name = "audio",
    srcs = [
        "main.c",
        "stack.s",
    ],
    copts = COPTS,
    visibility = ["//:__pkg__"],
    deps = [
        "//base",
        "//base:scheduler",
        "//n64:libultra",
        "@n64sdk//:gspF3DEX2.xbus",
        "@n64sdk//:rspboot",
    ],
)
