load(":defs.bzl", "models")

package(default_visibility = ["//assets:__pkg__"])

models(
    name = "logo",
    srcs = ["logo.fbx"],
    scale = "100",
    use_primitive_color = True,
)

models(
    name = "fairy",
    srcs = ["fairy.fbx"],
    scale = "50",
    use_normals = True,
)
