load(":defs.bzl", "models")

package(default_visibility = ["//assets:__pkg__"])

models(
    name = "logo",
    srcs = ["logo.fbx"],
    scale = "100",
    use_primitive_color = True,
)

models(
    name = "fairy",
    srcs = ["fairy.fbx"],
    scale = "2/11",  # Model is ~11 units tall.
    use_normals = True,
)
