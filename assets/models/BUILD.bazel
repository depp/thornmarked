package(default_visibility = ["//assets:__pkg__"])

genrule(
    name = "logo",
    srcs = ["logo.fbx"],
    outs = ["logo.dat"],
    cmd = "$(location //tools/modelconvert) -model $< -output $@ -scale 100",
    tools = ["//tools/modelconvert"],
)
